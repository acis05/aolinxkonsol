import { prisma } from '@/lib/db'
export default async function Companies(){const companies=await prisma.company.findMany({orderBy:{createdAt:'desc'}});return <><div className="top"><div><h1>Companies</h1><div className="muted">Database Accurate yang masuk konsolidasi</div></div></div><form className="card form" action="/api/companies" method="post"><input className="input" name="name" placeholder="PT Example" required/><input className="input" name="accurateHost" placeholder="https://...accurate.id" required/><input className="input" name="sessionId" placeholder="X-Session-ID" required/><button className="btn">Add Company</button></form><div className="card" style={{marginTop:16}}><table className="table"><thead><tr><th>Company</th><th>Host</th><th>Status</th><th>Sync</th></tr></thead><tbody>{companies.map(c=><tr key={c.id}><td><b>{c.name}</b></td><td>{c.accurateHost}</td><td><span className="pill">Active</span></td><td><form action={`/api/companies/${c.id}/sync`} method="post" className="form"><input className="input" name="from" placeholder="01/01/2026"/><input className="input" name="to" placeholder="31/12/2026"/><button className="btn secondary">Sync</button></form></td></tr>)}</tbody></table></div></>}
