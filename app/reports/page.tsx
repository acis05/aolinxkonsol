import { consolidatedReport } from '@/lib/consolidation/report'
export const dynamic = 'force-dynamic'

const fmt=(n:number)=>new Intl.NumberFormat('id-ID',{maximumFractionDigits:0}).format(n)
export default async function Reports({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;const from=q.from?new Date(q.from):new Date(new Date().getFullYear(),0,1);const to=q.to?new Date(q.to):new Date();const r=await consolidatedReport(from,to);return <><div className="top"><div><h1>Consolidated Reports</h1><div className="muted">Laba rugi / neraca berbasis reporting mapping</div></div></div><form className="card form"><input className="input" type="date" name="from"/><input className="input" type="date" name="to"/><button className="btn">Refresh</button></form><div className="card" style={{marginTop:16,overflowX:'auto'}}><table className="table"><thead><tr><th>Account Group</th>{r.companies.map(c=><th key={c.id}>{c.name}</th>)}<th>Total</th></tr></thead><tbody>{r.rows.map(row=><tr key={row.key}><td><b>{row.key}</b></td>{r.companies.map(c=><td key={c.id} style={{textAlign:'right'}}>{fmt(row.values[c.id]||0)}</td>)}<td style={{textAlign:'right'}}><b>{fmt(row.total)}</b></td></tr>)}</tbody></table></div></>}
