import { prisma } from '@/lib/db'
export const dynamic = 'force-dynamic'

export default async function Mappings(){const [companies,mappings]=await Promise.all([prisma.company.findMany(),prisma.accountMapping.findMany({include:{company:true},orderBy:{updatedAt:'desc'}})]);return <><div className="top"><div><h1>Account Mapping</h1><div className="muted">Mapping akun Accurate ke kelompok laporan konsolidasi</div></div></div><form className="card form" action="/api/mappings" method="post"><select className="input" name="companyId" required>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input className="input" name="accountNo" placeholder="1101" required/><input className="input" name="consolidatedKey" placeholder="Accounts Receivable" required/><select className="input" name="type"><option>REPORTING</option><option>INTERCOMPANY</option></select><input className="input" name="counterpartyId" placeholder="Counterparty company id (optional)"/><button className="btn">Save Mapping</button></form><div className="card" style={{marginTop:16}}><table className="table"><thead><tr><th>Company</th><th>Account</th><th>Type</th><th>Consolidated Key</th></tr></thead><tbody>{mappings.map(m=><tr key={m.id}><td>{m.company.name}</td><td>{m.accountNo}</td><td>{m.type}</td><td>{m.consolidatedKey}</td></tr>)}</tbody></table></div></>}
